CS50 DESIGNING NOTES

.SCHEMA (HOW A TABLE IS CREATED)
.SCHEMA TABLE NAME( TO KNOW THE SPECIFIC TABLE CREATION)

-NORMALIZATION(TO REDUCE THE REDUNDANCY) AND CREATING MORE TABLE WITH COMMON INFO.

CREATE TABLE KEYWORD.(TO CREATE A TABLE)
SYNTAX
CREATE TABLE "RIDERS" (
"ID" ,
"NAME"
)
CREATE TABLE "STATION" (
"ID" ,
"NAME",
"LINE"
)
CREATE TABLE "VISITS" (
"RIDER_ID" ,
"STATION_ID"
)
--DATA TYPES
--STORAGE TYPES(NULL,INTEGER,REAL,TEXT,BLOB-BINARY LARGE OBJECT- for example -images) NUMERIC- ADD ALL TYPES OF DATE AND TIME
(affinity)-also  known as data type

DROP TABLE "RIDERS";
.SCHEMA(RIDERS IS GONE)

CODE SCHEMA.SQL( PROVIDES ME A ENVIRONMENT TO TYPE A CODE OF SQL)-TERMINAL

CREATE TABLE "RIDERS"(
"ID" INTEGER,
"NAME" TEXT,
PRIMARY KEY("ID")
);

CREATE TABLE "STATIONS"(
"ID" INTEGER,
"NAME" TEXT NOT NULL UNIQUE,
"LINE" TEXT NOT NULL,
PRIMARY KEY ("ID")
}

CREATE TABLE "VISITS"(
"RIDER_ID" INTEGER ,
"STATITON_ID" INTEGER NOT NULL,
FOREIGN KEY ("RIDER_ID") REFERENCES "RIDERS"("ID"),
FOREIGN KEY("STATION_ID") REFERENCES "STATIONS"("ID")
)

}

IN TERMINAL-(.READ SCHEMA.SQL)

TABLE CONSTRAINTS
- PRIMARY KEY
- FOREEIGN KEY

COLUMN CONSTRAINTS
-CHECK( CHECK THE CONDITION BEFORE ADDING TO THE COLUMN)
FOR EXAMPLE:

"AMOUNT" NUMERIC NOT NULL CHECK("AMOUNT"!=0)- IF THE AMOUNT IS NOT EQUALS TO ZERO THEN THE VALUE IS ADDED TO AMOUNT COLUMN
"TYPE" TEXT NOT NULL CHECK("TYPE" IN ('ENTER','EXIT','DEPOSIT')- THE VALUE WITH THIS IS ONLY ADDED TO THIS

-DEFAULT (USED TO PROVIDE VALUE IF ANY VALUE IS NOT ENTERED)
FOR EXAMPLE
"DATETIME" NUMERIC NOT NULL DEFAULT CURRENT_TIMESTAMP,- IT MEANS IF NOT RECORDED IT SHOULD RECORD THE CURRENT TIME.

-NOT NULL 
-UNIQUE

#NEW ENTITY(CARD) CARD MAKES SWIPE AT STATION

WE DONT NEED RIDER TABLE SO, WE NEED TO CHANGE THE NAME USING ALTER

ALTER TABLE -KEYWORD
RENAME TO
ADD COLUMN
RENAME COLUMN -- TO --
DROP COLUMN

FOE EG:
ALTER TABLE "VISITS" RENAME TO "SWIPES";-RENAME TABLE NAME
ALTER TABLE "SWIPES" ADD COLUMN "TTPE" TEXT; (TYPE IS TYPED WRONG SO RENAME COLUMN)
ALTER TABLE "SWIPES" RENAME COLUMN "TTPE" TO "TYPE";
ALTER TABLE "SWIPES" DROP COLUMN "TYPE";-- TO REMOVE THE COLUMN "TYPE"


























